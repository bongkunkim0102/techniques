# Use the provider's exact HTTPS chat-completions endpoint and model ID.
TRANSLATION_ENDPOINT=https://stream.camelai.com/v1/chat/completions
TRANSLATION_MODEL=auto
# Optional: stop rather than accept a different provider-reported model.
TRANSLATION_EXPECTED_MODEL=
TRANSLATION_API_KEY=
